{
  "name": "turabakancs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/eloallit.js",
    "preview": "vite preview",
    "eles": "npm run build && npx --yes wrangler@latest dev",
    "kiad": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
